<h2> <%= @post.content %> </h2>
<h4><%= @post.is_published %></h4>

<h3>Comments <%= link_to "(+)", new_comment_path %></h3>

<ul>
  <% @post.comment.each do |comment| %>
    <li>
      <%= link_to "#{comment.body} (#{comment.body})", comment %>
    </li>
  <% end %>
</ul>
